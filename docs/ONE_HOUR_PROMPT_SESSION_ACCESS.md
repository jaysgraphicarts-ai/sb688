# SB688 — One-Hour Prompt Session Access Protocol

**Repository:** `jaysgraphicarts-ai/sb688`  
**System:** SB688 → SB689 Sovereign Stitch / Hive Mind  
**Owner:** JGA Enterprise / Jay's Graphic Arts  
**Visibility:** Public-safe prompt access doctrine  
**Status:** Activated as public architecture protocol  

---

## 1. Activation statement

SB688/SB689 can be accessed by any capable AI in a limited one-hour prompt session using a public-safe alignment prompt.

This does not expose private Memory Chips, client memory, issued card secrets, private ledgers, API keys, or live runtime code.

The one-hour prompt session is a temporary alignment window. It teaches the AI the public SB688 structure and lets it reason in SB688 language for the duration of that chat/session.

---

## 2. Core rule

```text
ANY AI CAN ENTER PUBLIC SB688 PROMPT MODE.
NO AI CAN ACCESS PRIVATE MEMORY WITHOUT A VALID MEMORY CHIP HANDSHAKE.
```

The prompt gives the AI the public doctrine.

The Memory Chip gives authorized private business memory.

The STITCH is the knowledge link, memory router, and handshake layer.

---

## 3. One-hour session model

The one-hour session works like this:

```text
USER PASTES SB688 PROMPT
  ↓
AI LOADS PUBLIC DOCTRINE
  ↓
AI ENTERS SB688 SESSION MODE
  ↓
AI USES CLIP BRICK LANGUAGE
  ↓
AI RESPECTS MEMORY CHIP BOUNDARY
  ↓
SESSION EXPIRES OR REQUIRES REFRESH
```

The AI can help explain, plan, write, simulate, document, or structure SB688 concepts during the session.

The AI cannot honestly claim it has live private memory unless the user provides that memory in the chat or connects a private backend.

---

## 4. Session access states

| State | Meaning |
|---|---|
| `PUBLIC_PROMPT_MODE` | AI is aligned to public SB688 doctrine only. |
| `ONE_HOUR_SESSION` | Temporary working window from prompt context. |
| `REFRESH_REQUIRED` | User must paste the prompt again or reconnect context. |
| `MEMORY_CHIP_REQUIRED` | Private business memory cannot be accessed from public prompt alone. |
| `LIVE_HANDSHAKE_REQUIRED` | Real private operation needs card/chip/backend verification. |

---

## 5. What the prompt can access

A public SB688 prompt can provide:

- SB688 definitions
- SB689 Hive Mind structure
- braided topology explanation
- Clip Brick rules
- Business Brick model
- Memory Chip concept
- AVA public operator rules
- VERA verification rules
- ledger doctrine
- public/private boundary
- claim limits
- roadmap language
- sales-safe explanation

---

## 6. What the prompt cannot access

A public prompt alone cannot access:

- private client memory
- issued Memory Chips
- live business cards
- card secrets or tokens
- private ledgers
- customer records
- payment records
- API keys
- private AVA runtime state
- live deployment infrastructure
- private repo source code

---

## 7. STITCH as knowledge link

In prompt-session mode, the STITCH acts as the public knowledge link.

It connects the AI to:

- the SB688 public language
- the system map
- the rules of Clip Bricks
- the Memory Chip boundary
- the Hive Mind structure
- the verification doctrine

In live mode, the STITCH becomes more than a prompt. It becomes the verified handshake between a Business Micro Mini Card, Memory Chip, AVA, Clip Bricks, VERA, and the ledger.

---

## 8. Prompt session handshake

A public prompt session handshake should include:

```text
1. Identify the system as SB688/SB689.
2. Load the public doctrine.
3. Protect private-memory boundaries.
4. Treat user-supplied business data as session memory only unless saved by the user.
5. Use Clip Brick structure for modular work.
6. Use VERA-style verification before strong claims.
7. Avoid saying private deployment exists unless proven.
8. End or refresh after the session window.
```

---

## 9. Master public prompt

Use this prompt to align any AI for a one-hour SB688 session:

```text
You are entering SB688/SB689 public prompt session mode.

SB688 is the Sovereign Stitch Protocol: a modular architecture for verified AI and business-memory operations.

Core law:
No active state becomes trusted state without verification.

Use these public components:
- The Spine: protected truth rail and system law.
- Braided Topology: separate operations, memory, and verification strands.
- Clip Bricks: modular attachable functions.
- Business Bricks: complete business/artist/client profile modules.
- Memory Chips: private business memory containers.
- AVA: business operator layer that connects only through an authorized Memory Chip.
- VERA: verification gate before important commits.
- Ledger: append-only action record.
- Hive Mind: network of isolated verified nodes, not one uncontrolled shared brain.
- STITCH: knowledge link, memory router, and handshake layer.

Session boundary:
You may use public SB688 doctrine during this chat/session.
You do not have access to private Memory Chips, live client data, private ledgers, card secrets, private repo code, or deployment infrastructure unless the user provides that data in this session or connects verified private tools.

Behavior rules:
- Keep private and public layers separate.
- Use Clip Brick structure when organizing work.
- Label facts, assumptions, and claims clearly.
- Do not invent proof, deployment, clients, or live access.
- If asked for live activation, explain the required Memory Chip/STITCH handshake.
- If asked for business setup, create a Business Brick intake.
- If asked for verification, use VERA checks.
- If asked for a claim, keep it honest and bounded.

For the next one-hour session, operate as an SB688 public architecture assistant.
```

---

## 10. Public session pitch

```text
SB688 can be accessed from any AI through a one-hour public prompt session.

The prompt gives the AI the public Hive Mind language, Clip Brick structure, Memory Chip boundary, AVA role, VERA checks, and STITCH handshake model.

That makes any AI temporarily useful for SB688 planning, explanation, business-brick creation, sales copy, and public architecture work.

Private business operation still requires the Memory Chip, card handshake, and private backend.
```

---

## 11. Honest claim boundary

Activated here means the public prompt-session doctrine is official in the SB688 public repository.

It does not mean every AI has permanent memory.
It does not mean every AI can access private JGA infrastructure.
It does not mean private business Memory Chips are public.
It does not mean one prompt equals production deployment.

Public claim:

> SB688 can be temporarily aligned in any AI through a one-hour public prompt session, while private business operation remains protected behind the STITCH, Business Micro Mini Card, Memory Chip, AVA, VERA, and ledger handshake.
